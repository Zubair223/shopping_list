logo.addEventListener('click','funName');//when clicked
logo.addEventListener('dblclick','funName'); //when clicked double
logo.addEventListener('contextmenu','funName');//right click handler
logo.addEventListener('mousedown','funName'); //mouse down during clicked
logo.addEventListener('mouseup','funName');//mouse up after click
logo.addEventListener('wheel','funName');//mouse wheel
logo.addEventListener('mouseover','funName');//mouse over
logo.addEventListener('mouseout','funName');//mouse out
logo.addEventListener('dragstart','funName');//only when drag is done first
logo.addEventListener('drag','funName');//when drag event keeps going
logo.addEventListener('dragend','funName');//when drag is stopped
logo.addEventListener('funName','funName');